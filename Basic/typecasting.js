// Typecasting : conversion of one data type to other 
"use strict" // it use new version of javascript 

let score = "33abs"

let mark = Number(score);
//console.table([typeof(mark) , mark]) // type gives you Number but in case of mark is give NaN meanse Not a number 


let boolnum = "False" ;
let boolvar = Boolean(boolnum);
//console.table([typeof(boolvar) , boolvar]);

// let's understand arithematic operation  
//console.log(5+4);
let s = 3 + 'Yuvr';
//console.log(s);

// Comparison Operator convert Null to 0 
 
// console.log( null > 0)
// console.log( null == 0)
// console.log( null >= 0)

// strictly check Operator (===) it check value or data typer of variable 
// console.log("2" == 2)  // it gives you true 
// console.log( "2" === 2)  // it gives you false it checks data type as well 


